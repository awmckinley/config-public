# yaml-language-server: $schema=https://aka.ms/configuration-dsc-schema/0.2
---
properties:
  configurationVersion: 0.2.0
  resources:
    # replaces: AmN.yasb
    # replaces: Rainmeter.Rainmeter
    - id: glzr-io.zebar
      resource: Microsoft.WinGet.DSC/WinGetPackage
      directives:
        description: Install Zebar
      settings:
        id: glzr-io.zebar
        source: winget
